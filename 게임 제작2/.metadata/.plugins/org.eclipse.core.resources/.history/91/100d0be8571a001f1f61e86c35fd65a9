package Object;

import java.io.IOException;

import javax.imageio.ImageIO;

public class OBJ_BOX_RIGHT extends SuperObjects {
	public OBJ_BOX_RIGHT() {

		name = "닫힌 상자 오른쪽";
		try {
			image = ImageIO.read(getClass().getResourceAsStream("/Objects/닫힌 상자_오른쪽.png"));
		} catch (IOException e) {

			e.printStackTrace();
		}
		collision = true;
	}
}
